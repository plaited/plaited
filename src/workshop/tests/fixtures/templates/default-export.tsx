import type { FunctionTemplate } from 'plaited'

const DefaultTemplate: FunctionTemplate = () => (
  <div>
    <h1>Default Export Template</h1>
    <p>This is exported as default</p>
  </div>
)

export default DefaultTemplate
