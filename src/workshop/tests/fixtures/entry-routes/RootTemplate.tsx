import type { FunctionTemplate } from '../../../../main.js'

/**
 * Root-level template for testing get-entry-routes path transformation.
 * Expected bundle path: /root-template--index.js
 */
export const RootTemplate: FunctionTemplate = () => <div>Root Level Template</div>
