// import { expect, test, describe } from 'bun:test'
// test.each([
//   [1, 2, 3, 1000], // 1 second timeout
//   [2, 4, 6, 2000], // 2 second timeout
//   [3, 6, 9, 5000], // 5 second timeout (default)
// ])('adds %i + %i to equal %i', (a, b, expected, timeout) => {
//   expect(a + b).toBe(expected);
// }, { timeout:  });

// describe.each([
//   [1, 2, 3, 1000], // 1 second timeout
//   [2, 4, 6, 2000], // 2 second timeout
//   [3, 6, 9, 5000], // 5 second timeout (default)
// ])('', (route, timeout, expected) => {
//   test('', async() => {

//   }, timeout)
// })
