# Plaited Workshop

## Server

- start server probably not a tool call it starts simply by connecting server
- set working root or get permission to read and write files to a root directory
- Run server in watch mode
- Stop server

## Stories

- get stories for template(s)
- get all stories

## Design Tokens

- build design tokens

  - watch for token changes
- re/set design token directory
- get/list design tokens
- filter design tokens (this one is fun as it's allowing for dynamic code injection)
  - or we could just provide default filter options the most common one is probably a dep check
- check if design token has a token
- create design token element

## Components

- get a list of functional templates
- get a list of plaited templates
- create a functional template
- create a plaited template
