// import type { Trigger } from '../behavioral.ts'
// import { AGENT_CORE_EVENTS } from './agent.constants'

// const DEFAULT_HEARTBEAT_INTERVAL_MS = 15 * 60 * 1000

// const DEFAULT_TOOL_TIMEOUT_MS = 120_000
// const DEFAULT_CONTEXT_MEMORY_TTL_MS = 15 * 60 * 1000

// export const usePulse = ({ path, schedule, trigger: Trigger }) => Bun.cron('', async () => {})
