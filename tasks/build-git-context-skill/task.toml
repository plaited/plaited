schema_version = "1.4"

[task]
name = "behavioral/build-git-context-skill"
version = "1.0.0"
description = "Author and install an AgentSkills-compliant git-context skill (SKILL.md + JSON-in/JSON-out CLI script) at a conventional discovery location, verified against hermetic git fixtures"
keywords = ["agent-skills", "cli", "git", "json", "tooling"]

[[task.authors]]
name = "Behavioral"
email = "agents@behavioral.sh"

[metadata]
difficulty = "hard"
category = "programming"
tags = ["agent-skills", "cli", "git", "json"]

[verifier]
timeout_sec = 900.0

[agent]
timeout_sec = 3600.0

[environment]
network_mode = "no-network"
build_timeout_sec = 900.0
os = "linux"
workdir = "/app"
