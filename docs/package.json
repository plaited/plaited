{
  "name": "@plaited/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/index.ts",
    "test": "bun test scripts/**/*.spec.ts"
  },
  "devDependencies": {
    "plaited": "workspace:*",
    "@plaited/utils": "workspace:*"
  }
}
