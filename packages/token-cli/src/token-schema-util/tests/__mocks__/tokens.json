{
  "platformSpaces": {
    "Mobile": {
      "verticalSpace": {}
    }
  },
  "s": {
    "$value": "{spaces.space-8}",
    "type": "spacing",
    "description": "Rows & Section’s Title - Mobile. Two different type sizes are baseline aligned and then vertically spaced"
  },
  "Small": {
    "$value": [
      {
        "color": "rgba({color.systemGray.0}, {opacities.opacity-32})",
        "type": "dropShadow",
        "x": "{axes.axis-0}",
        "y": "{axes.axis-0}",
        "blur": "{blurs.blur-4}",
        "spread": "{spreads.spread-0}"
      },
      {
        "color": "rgba({color.systemGray.0}, {opacities.opacity-16})",
        "type": "dropShadow",
        "x": "{axes.axis-0}",
        "y": "{axes.axis-0}",
        "blur": "{blurs.blur-8}",
        "spread": "{spreads.spread-0}"
      }
    ],
    "type": "boxShadow",
    "description": "Drop shadows are used to bring depth and hierarchy to the UI. There are three sets of drop shadows for both dark and light themes. To achieve the proper result, there are two layers of shadow on top of each other. General Note: Drop shadows are only used on non-interactive containers, not on the UI Components. "
  }
}