name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  # Detect which paths changed to conditionally run expensive jobs
  changes:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      acp: ${{ steps.filter.outputs.acp }}
      agent: ${{ steps.filter.outputs.agent }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            acp:
              - 'src/acp/**'
              - 'src/acp.ts'
            agent:
              - 'src/agent/**'
              - 'src/agent.ts'

  test-pr:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install dependencies
        run: |
          bun install
          bunx playwright install chromium --with-deps
      - name: Run check
        run: |
          bun run check
      - name: Run test
        run: |
          bun run test

  # Agent Docker tests (sqlite-vec, tool discovery)
  # Only runs when src/agent/ files change
  test-agent-docker:
    needs: changes
    if: ${{ needs.changes.outputs.agent == 'true' }}
    runs-on: ubuntu-latest
    container:
      image: oven/bun:1.2.9
      options: --user root

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run agent Docker tests
        run: bun test ./src/agent/tests/skill-discovery.docker.ts ./src/agent/tests/tool-discovery.docker.ts

  # ACP integration tests run in Docker container for consistent environment
  # Only runs when src/acp/ files change to reduce API costs
  test-acp-integration:
    needs: changes
    if: ${{ needs.changes.outputs.acp == 'true' }}
    runs-on: ubuntu-latest
    container:
      image: oven/bun:1.2.9
      options: --user root

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run ACP integration tests
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: bun test ./src/acp/tests/acp-integration.docker.ts
