import ts from 'typescript'
import { Ollama } from 'ollama'
import path from 'path'
import fs from 'fs'

export const parseStoryFile = async (
  source: string,
): {
  ast
  links
  source
  embeddings
} => {}
