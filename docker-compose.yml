services:
  deno:
    container_name: "plaited_tests"
    entrypoint: deno
    stdin_open: true # docker run -i
    tty: true # docker run -t
    build:
      context: .
      args:
        - TAG=v1.31.3
    ports:
      - 3000:3000
      - 9000:9000
    volumes:
      - .:/deno-dir:delegated