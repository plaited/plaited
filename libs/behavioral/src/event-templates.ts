// import { BPEvent, BPEventTemplate } from "./types.js";

// export const randomEvent: BPEventTemplate = (...events:BPEvent[]) => events[
//   Math.floor(Math.random() * Math.floor(events.length))
// ]